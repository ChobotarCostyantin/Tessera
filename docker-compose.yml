version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: tessera_postgres
    restart: always
    env_file:
      - "backend/.env"
    ports:
      - "5432:5432"
    volumes:
      - tessera_pg_data:/var/lib/postgresql/data

volumes:
  tessera_pg_data: